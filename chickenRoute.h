#ifndef HEADER_FILENAME
#define HEADER_FILENAME

void clearInput();
int inp(int x, int y, int z);
void festival(int* m, INVENTORY* i);
int bet(int q);
void job(int* hp, int* money);
void chickenRoute(int* health, int* money, Inventory *inv);

#endif
